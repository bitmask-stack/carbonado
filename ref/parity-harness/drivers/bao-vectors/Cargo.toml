[package]
name = "bao-vectors"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
blake3 = { version = "1", default-features = false, features = ["std"] }
hex = "0.4"
bao-tree = { path = "../../../bao-tree", default-features = false, features = ["validate"] }
