[package]
name = "carbonado-sys"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "FFI bindings to Lean AOT libcarbonado (dual-backend parity)"
publish = false

[dependencies]

# When enabled (by carbonado `backend-lean`), fail the build if CARBONADO_LEAN_LIB is unset
# or does not contain libcarbonado shared/static artifacts.
[features]
require-lib = []

[build-dependencies]
